const SRC = 'src/'
const DIST = 'dist'
const SCRIPTS = 'scripts/app/'
const STYLES = 'themes/app/'
const PAGES = ['pay-confirm', 'guide', 'qa', 'protocol', 'how']
const CDN = '/'

module.exports = {
  SRC,
  DIST,
  SCRIPTS,
  STYLES,
  PAGES,
  CDN
}
